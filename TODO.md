# TODO: Run CropCycle Logistics Project

## Steps:
1. [x] Install backend dependencies (`cd backend && npm install`)
2. [x] Install frontend dependencies (`cd frontendneew && npm install`)
3. [x] Confirm/setup MONGO_URI in backend/.env (mongodb://127.0.0.1:27017/cropcycle)
4. [x] Start backend server (`cd backend && npm run dev`) - Running (check terminal for DB connection status)
5. [x] Start frontend dev server (`cd frontendneew && npm start`) - Running (localhost:3000, auto-opens browser)
6. [x] Verify both running (backend:5000, frontend:3000)

**Notes:** Backend requires MongoDB connection. Provide MONGO_URI if not in .env. Frontend proxies API calls.
